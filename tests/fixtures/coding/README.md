# Controlled coding acceptance repository

This directory supplies a small, public, secret-free source snapshot for real sandbox acceptance.
The test changes `calc.py`, runs an assertion, and exports the changed file.
